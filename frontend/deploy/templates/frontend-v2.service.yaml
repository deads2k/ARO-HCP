apiVersion: v1
kind: Service
metadata:
  labels:
    app: aro-hcp-frontend-v2
  name: aro-hcp-frontend-v2
  namespace: '{{ .Release.Namespace }}'
spec:
  ports:
  - port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    app: aro-hcp-frontend-v2
  type: ClusterIP
